export interface WelcomeSectionProps {
    // Empty for now since the component doesn't take props
    // But we can add props later if needed, like:
    // title?: string;
    // subtitle?: string;
    // logoSrc?: string;
}